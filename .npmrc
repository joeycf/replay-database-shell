# @vercel/speed-insights@2 declares peerOptional vue-router@^4; Nuxt 4 ships
# vue-router@5 (API-compatible for what the module uses). Until the peer range
# catches up, installs everywhere (local, CI, Vercel) must not hard-fail on it.
# Replicated verbatim from the game repos (STACK §1 consumer contract).
legacy-peer-deps=true
