{
  // Solution-style root (engine pattern, STACK.md §2): extends the canonical
  // baseline and delegates the typecheck to the Nuxt-4 project-references
  // contexts generated by `nuxt prepare`. `npm run typecheck` runs this via
  // `nuxt typecheck` (vue-tsc -b). No pipeline track here — the shell has no
  // data pipeline (unlike the game repos).
  "extends": "./tsconfig.base.json",
  "files": [],
  "references": [
    { "path": "./.nuxt/tsconfig.app.json" },
    { "path": "./.nuxt/tsconfig.node.json" },
    { "path": "./.nuxt/tsconfig.shared.json" },
    { "path": "./.nuxt/tsconfig.server.json" }
  ]
}
